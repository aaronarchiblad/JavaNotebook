1. JDK, JRE, JVM三者之间的关系, 以及JDK, JRE包含的主要结构有哪些
ANS: JDK包含JRE包含JVM. JDK包含了JRE以及开发Java程序需要的组件,
如: javac, java, javadoc. JRE包含了JVM以及一些Java的核心类库.

2. 为什么要配置path环境变量? 如何配置?
因为想要在任何路径下面都可以通过java/javac等命令运行. 设置JAVA_HOME
的变量, 用来存放bin的上一级目录. 再在path里动态的调用JAVA_HOME

